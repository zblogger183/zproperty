@AGENTS.md

# Real Estate Content Ops — Status

## Data model in use
- `projects` = individual developments (new-build towers, mixed-use, plot schemes), each with a `developer_id` (a real Supabase Auth account) and `payment_plans` rows, sourced from the developer's own site/PDF where reachable rather than a broker's summary page.
- `societies` = master-planned housing schemes (DHA, Bahria Town, Al-Kabir Town, Etihad Town, etc.), each with a public profile at `/area-guide/[city]/[society]`. `projects.society_id` links a project to its society when it genuinely sits inside one — an address on an organic city neighborhood (Gulberg III, Garden Town, Johar Town, Defence Road, Bedian Road...) is correctly left `society_id: null`, that's not a gap.
- Cross-linking (added this session): the area-guide page now shows "New Projects in [Society]" (queries `projects` by `society_id`, `app/(public)/area-guide/[city]/[society]/page.tsx`); the project detail page now shows "Part of [Society] →" linking back (`app/(public)/new-projects/[slug]/page.tsx`). Before this, `projects.society_id` was populated but never rendered anywhere.

## Closed this session
- **Admin UI for societies** now exists: `/admin/societies` (list) and `/admin/societies/new` (create form + `createSocietyAction` in `app/admin/societies/actions.ts`), mirroring the `/admin/projects` pattern. Sidebar link added in `AdminSidebar.tsx`.
- **`areas` table**: turns out Islamabad and Karachi already had ~8 areas each (an earlier multi-statement `execute_sql` call only surfaced the *last* query's result, which read as "empty" and produced a wrong note here — multi-statement Supabase calls only return the final statement's rows, don't trust an empty-looking result from one without re-checking). Only **Multan was genuinely empty** and got 3 areas (Bosan Road, Cantt, DHA Multan). Also added `B-17` for Islamabad. Linked with confidence: Multi Gardens B-17 → `B-17`, Inter City Housing Scheme → `Bosan Road`, Bahria Town Islamabad/Karachi + DHA Islamabad/Karachi societies → their matching existing areas. Left `area_id: null` on Faisal Hills, University Town, and Park View City societies rather than guess their containing area/sector.
- **Society entity schema**: `societySchema()` added to `lib/seo/schemas.tsx` (`Place` type, geo + `additionalProperty` for developer/established/plots/phases/avg price), wired into the area-guide page alongside the existing conditional `FAQPage` schema.
- **Society edit page + real image upload**: `/admin/societies/[id]` now exists (`SocietyForm` in `components/admin/SocietyCreateForm.tsx` handles both create and edit via an optional `initial` prop). Cover image and `gallery_images` now go through `ImageUploader` (Cloudinary, `folder="societies"`) instead of a pasted URL — the previously-dead `gallery_images` column is now populated from the admin and rendered publicly via `ImageGallery` on the area-guide page.
- **Listing submission blocked for all agents, fixed**: `permission denied for sequence listing_number_seq` — `017_listing_number.sql` created the sequence but never granted `USAGE` on it to any role; a sequence's privileges are independent of the owning table's grants/RLS, so even the service-role admin client hit this. Fixed live + `020_listing_number_seq_grants.sql`.
- **Homepage/hero stats fixed** (`get_public_stats()`, called by `lib/stats.ts`): was showing "1 Listing · 5 Agents · 1 City" — this function was live in the DB with **no migration file in the repo at all** (now captured, see `021_public_stats_overall_data.sql`). Two stacked bugs: `listings_count` only counted the thin `listings` table and ignored `projects` (63 active rows, the bulk of real site content); `cities_count` used `cities.listing_count`, a plain `INTEGER DEFAULT 0` column **no trigger anywhere ever increments** — a dead counter, not a live one. Now: `listings_count` = active listings + active projects; `cities_count` = live distinct-city count across active listings, active projects, and societies. Confirmed live: `65 · 5 · 4 · 52`. **Takeaway: if a live DB function/trigger has no matching file under `supabase/migrations/`, don't assume it's correct just because it's in production — check with `grep -rln` before trusting it.**

- **7 new projects added** (Faisal Heights Apartments, Radisson BLU Hotel & Residences Islamabad, Indigo Signature Apartments, HS Ideal Tower, Calachi Icon, Reportage Residences, Pine Hills Residencia 2) with 6 new developer accounts (Faisal Town Group, Indigo Developers, Hafiz Sajjad Builders, Zifan Group & Property Point, Reportage Group, Capital Movers & Builders) — Radisson BLU reused the existing J7 Group account. Added `Faisal Town` and `Mumtaz City` as new Islamabad areas. Linked HS Ideal Tower → Bahria Town Lahore society, Pine Hills Residencia 2 → DHA Islamabad society. **`thebase.pk` was skipped** — it's a broker/consultant site, not a project, and its flagship listing (B45, MM Alam Gulberg-III) was already in the DB under the real developer, Prime Builders & Developers. **Calachi Icon sits inside "Calachi Housing Society"**, National Stadium Road, Gulshan-e-Iqbal, Karachi — not yet its own `societies` row (left `society_id: null`, noted in the project description), same treatment as the Skyline Boulevard/Hassan Villas gaps below. Indigo Signature Apartments has no reliable structured pricing/payment-plan published anywhere reachable — added with confirmed developer/location only, pricing left null rather than guessed.

- **Developer self-service dashboard added** — every developer account created this session (and earlier) landed on `/dashboard`, which was entirely agent/`listings`-shaped (`listings.agent_id`, `agent_profiles`, sidebar nav "My Listings"/"CRM"/"Boost Center"). Developers own zero `listings` rows — their content is `projects` keyed by `developer_id` — so the dashboard was structurally empty for every developer, not a bug in any one query. Fixed by making the whole `/dashboard` tree role-aware (`DashboardSidebar.tsx` branches on `role === "developer"`; `app/dashboard/page.tsx` renders a separate `DeveloperOverview`) and adding a parallel developer-facing project CRUD: `/dashboard/projects` (list), `/dashboard/projects/new` + `/dashboard/projects/[id]/edit` (`ProjectForm` in `components/dashboard/ProjectForm.tsx`, reusing `Step2Location` and `ImageUploader`), actions in `app/dashboard/projects/actions.ts` (`verifyDeveloper()`, ownership-checked like `verifyAdmin()`). Developer-submitted projects go in as `status_platform: "pending"` and land in the *existing* `/admin/projects/pending` review queue automatically — no admin-side changes needed, that queue was already generic. `/dashboard/leads` and the lead cards (`LeadCard.tsx`, `RecentLeadsCard.tsx`) were also made role-aware (`leads.developer_id` vs `leads.agent_id` — the `project_id`/`developer_id` columns and the notification-on-lead code already existed in `app/api/projects/[id]/lead/route.ts`, they just weren't surfaced anywhere a developer could see them).

- **2 new projects added** (DHA Condominiums, The Pavilion) with 2 new developer accounts (Suria Developers, Aasif Developers). DHA Condominiums → linked to the existing DHA Lahore society; The Pavilion → linked to the existing Bahria Town Lahore society. Neither project has published pricing anywhere reachable (developer sites explicitly withhold it, directing buyers to contact sales) — both added with confirmed developer/location/unit-type/size data only, no `payment_plans` rows. The Pavilion is a 3-developer joint venture (Aasif Developers, Brick by Brick Developers, 3B Developers) — `developer_id` set to Aasif Developers (listed first/primary across sources), partners noted in the description.
- **Fixed a real bug surfaced by this session's own earlier work**: `projects.amenities` defaulted to `'{}'` (an empty JSON *object*) at the schema level instead of `'[]'` (an empty array) — copy/paste drift in `001_schema.sql` from a different table (`societies.amenities` correctly defaults to `'[]'`). Every part of the app treats `amenities` as a `string[]`; any insert that omitted the column (a direct SQL insert, unlike the app's own forms) silently got the wrong shape. This crashed the new developer-facing `/dashboard/projects/[id]/edit` page (`.join()` on a plain object) for 4 projects inserted earlier this session without an explicit `amenities` value. Fixed the column default (`022_fix_projects_amenities_default.sql`), backfilled the 4 bad rows to `'[]'`, and added an `Array.isArray()` guard on the edit page matching the pattern the public project page already used. **Takeaway: when inserting a `projects` row via direct SQL, always pass `amenities` explicitly (even `'[]'::jsonb`) — don't rely on the column default.**

- **Pending listings invisible to admins despite real listings queued**: `/admin/listings/pending` showed "0 awaiting review" while the admin dashboard's stat card correctly said "3 Pending Approval" for the same table. Root cause: `013_society_phases_blocks.sql` granted `SELECT`/`INSERT` on `society_phases`/`society_blocks` to `anon`/`authenticated` only — never to `service_role`. RLS bypass and table GRANTs are separate layers; `service_role` bypasses the RLS policies but still needs its own GRANT to touch a table at all (same root cause as the `listing_number_seq` bug fixed earlier this session). The pending-listings page embeds `phase:society_phases(name)` and `block:society_blocks(name)` via the admin (service_role) client — with no grant, the whole query errored, and the code only destructured `{ data }`, silently discarding `error` and falling back to an empty array. Fixed live + `023_society_phases_blocks_service_role_grants.sql`; also added `console.error` logging on that query so a future failure shows up in runtime logs instead of looking like "nothing pending". **Takeaway: this is the second time a table/sequence created in a later migration turned out to be missing a `service_role` grant that earlier, foundational tables already had — when adding a new table, diff its grants against an existing table's (`information_schema.role_table_grants`) rather than assuming Supabase's default privileges cover it.**

- **Imarat Downtown updated with Mall of Imarat Tower 1 pricing** — the project already existed (an umbrella entry covering several towers under one masterplan), with `payment_plans` rows for Hospitality Tower 1, Imarat Grand Bazar, and Imarat Residences 2, but nothing for Mall of Imarat Tower 1 specifically. Added 9 new rows (Ground/1st/2nd/3rd floor retail, 5th floor cinema/food court net & gross, Executive Suites, Offices shell-and-core and finished) sourced from the developer's own "Mall of Imarat Payment Plan 2026" page — real per-sqft rates, no down-payment %/installment schedule published so those fields stay null per convention. Project now has 16 payment_plans rows total. Detail page has a 10-minute ISR cache (`revalidate = 600`), so this shows up without a redeploy.

- **1 new project added: Giga Mall Extension** (DHA Phase 2, Islamabad), linked to the existing DHA Islamabad society. Developer attributed to **Al Ghurair Giga** (confirmed via a Zameen News article, not a broker page — "Al Ghurair Giga Pakistan Private Company," a JV between Dubai's Al Ghurair Group and Pakistan's Giga Group), not the broker page (isbproperty.com) the user linked. 6 payment-plan rows: real total prices from isbproperty.com/paymentplan cross-checked against propertynaama.com's independent per-sqft listing (consistent), with the 25%-down/16-quarterly-installment structure from propertynaama (isbproperty didn't state the down-payment %). Per-installment amounts are **computed** from those two published facts (total price × structure), not separately published — noted as such in each row.

- **1 new project added: MGC Jewel** (Bahria Town Phase 8, Rawalpindi) — developer: MGC Developments (Mian Group of Chakwal, confirmed via their own official site). **First project ever added for Rawalpindi**, so also added `Bahria Town` as Rawalpindi's first `areas` row; left `society_id: null` rather than guess whether it should join the existing (Islamabad-city) Bahria Town Islamabad society or warrant its own Rawalpindi-city society row (Zameen.com's own taxonomy does treat "Bahria Town Rawalpindi" as distinct). **No payment_plans rows added** — every third-party broker source found gives materially conflicting unit sizes (roughly 500-1,900 sq.ft depending on source), down-payment % (15-25%), and plan length (6-8 years), with no single reliable source stating exact total prices; recorded as structural/amenity facts only, with the pricing inconsistency flagged explicitly in the description rather than picking one source arbitrarily.

- **1 new project added: RJ's Lifestyle Residences** ("LAROM Hotel & Residences by Continent" branded, Bahria Lifestyle, Islamabad Expressway), developer RJ Developers & Infrastructure Company. **"Bahria Lifestyle" is a Bahria Town Pvt. Ltd.-developed mixed-commercial corridor along the Expressway, distinct from the main phase-numbered "Bahria Town Islamabad" society** — linked to the existing "Bahria Town" area but deliberately left `society_id: null` rather than conflate the two. 6 unit types (A-F, 755-971 sq.ft, confirmed via the developer's own investor site) recorded with no fixed `total_price` — only a pre-completion per-sqft range (PKR 18,000-22,000) is published; exact unit pricing is gated behind a "Register Interest" form, so `payment_plans` rows carry sizes/notes only, no fabricated totals.

- **1 new project added: Islamabad Square** (entrance of Multi Gardens B-17, Islamabad), developer Ovaisco & Century Builders — linked to the existing Multi Gardens B-17 society (clean, unambiguous match, unlike several earlier cases). Pricing is floor-tiered (Rs 9,200/sqft on the 1st floor vs. Rs 7,600/sqft on the 4th floor and above) rather than fixed per unit type; recorded as 2 floor-tier `payment_plans` rows with no fixed `total_price` since no reliable single price table exists per unit type — a specific broker-quoted example (1,720 sqft/Rs 1.376 Cr with a booking+installment breakdown) didn't reconcile internally (components summed to well short of the stated total), so it was excluded rather than risk propagating an error.

- **1 new project added: Atrium Hills** (adjacent to Zaraj Housing Scheme, Sector B, Islamabad), developer Seven Arz Development Group — a small/new developer with no third-party press coverage found, but with named leadership (CEO/Founder + Executive Director) on their own dedicated project site, which is the developer's own site (no broker-attribution question here). **Deliberately not confused with "The Atrium"**, an apparently different, older project also near Zaraj Housing Scheme (different address framing, different unit-tier names/sizes, no developer link found between the two) — used only Atrium Hills' own site data, not The Atrium's pricing. No pricing, floor count, or completion timeline published anywhere — added with confirmed developer/location/5-unit-type/size data only. Zaraj Housing Scheme isn't yet catalogued as a society, so `area_id`/`society_id` both left `null` (a new case for the ambiguous-location gap list below).

- **4 new projects added** (Emaar Park Edge, Tribeca Karachi, Grand Monarch Seaside Residency, Harsukh Residencies) with 4 new developer accounts (Emaar Pakistan, Mall Developers, AL Wakeel Group & Bakshi's Builders, Almaymaar). `aeontrisl.pk` was the source for 3 of these (Emaar Park Edge, Tribeca, Grand Monarch) and is a **marketing/broker site, not a developer** — verified the real developer for each independently: Emaar Park Edge and Tribeca both check out via their own official sites (`pk.emaar.com`, `tribeca-karachi.com`); **Grand Monarch's aeontrisl.pk page claimed Aeon & Trisl itself as the developer, which independent sources contradict** (real developer: AL Wakeel Group & Bakshi's Builders JV) — a textbook case of the broker-self-attribution trap this session's conventions warn about. Emaar Park Edge and Tribeca both linked to the existing DHA Karachi society; Grand Monarch linked to the existing Clifton area only (organic neighborhood, no society). **Harsukh Residencies (Nathiagali) required a new city**: it's in Nathiagali, Galiyat, Abbottabad District, KPK — not one of the 10 existing cities. Asked the user how to model it; they chose **Abbottabad as the city, Nathiagali as an area within it** (rather than Nathiagali as its own top-level city) — both newly created. No pricing/payment-plan totals were found for Tribeca or Harsukh (both genuinely unpublished, "upon request"); Emaar Park Edge and Grand Monarch had real published starting prices.

- **6 new projects added for Q-Links Property Management** (Pine Homes, Pine Enclave, Q-High Street, Jasmine Grand Mall, Orchard Mall, Broadway Shopping Centre), 1 new developer account. `qlinks.pk` is the developer's own site (not a broker), so all 6 came from a single verified source. Jasmine Grand Mall and Orchard Mall linked to the existing Bahria Town Lahore / Bahria Orchard societies respectively; Broadway Shopping Centre also linked to Bahria Orchard. **Pine Homes, Pine Enclave, and Q-High Street are on "Pine Avenue (Eden Abad)" and "Engineers Town"** — neither is yet catalogued as an area/society, left `area_id`/`society_id` null on all three (a new gap, see below). Note: this session already had an unrelated "Pine Homes" project from a different developer (Barir Developers) — the two are genuinely distinct projects that happen to share a name, not a duplicate. Broadway Shopping Centre has real floor-tiered per-sqft rates (Rs 65,000 ground floor / Rs 44,000 1st floor) plus a 30%/36-monthly/20%-possession structure, but a specific broker-quoted total-price example didn't reconcile internally and was excluded. The other 5 projects have no published pricing anywhere reachable.

- **1 new project added: Tower 10** (G-9 Markaz, Islamabad), developer Taseer Real Estate Builders & Developers — confirmed via search as a genuine, established developer (other projects: Taseer Hilltop Heights in Bahria Town Phase 8 Rawalpindi, Taseer Heights in Murree) even though no third-party coverage of Tower 10 itself was found; tower10.com.pk is the project's own dedicated site, not a broker page. Added `G-9` as a new Islamabad area (a standard CDA sector, same tier as the existing F-10/F-7/G-11 entries — low-risk addition, unlike the ambiguous corridor cases). No unit sizes, pricing, payment plan, or completion timeline published anywhere — added with confirmed developer/location/property-type data only.

- **New-listing submission showed a bare "Invalid listing data" with no field detail** — reported live via screenshot on a 5-Marla plot listing (DHA Phase 9 Town, Block A, Lahore) failing at the final "Submit for Review" step. `POST /api/listings` (`app/api/listings/route.ts`) already returns `{ error: "Invalid listing data", details: parsed.error.flatten() }` on a Zod rejection, but `ListingForm.tsx`'s `handleSubmit()` only read `result.error` and discarded `result.details` — the THIRD instance this session of the same "silently swallowed error" pattern (after the `listing_number_seq` grant bug and the `society_phases`/`society_blocks` service_role grant bug). Fixed by extracting `result.details.fieldErrors` and appending a `"field: message"` summary to the thrown error. Traced the whole client/server validation chain first (`app/dashboard/listings/schemas.ts`'s six step schemas, merged identically into `createListingSchema` and imported by both `ListingForm.tsx` and the API route; the step wizard's Next/Back buttons are the only navigation — `StepIndicator` has no click handler, so no step can be skipped) and found no deterministic schema mismatch — client and server validate the exact same merged shape, so a normal forward-only submission that passes every step's own check must pass server-side too. Likely explanation for this specific failure: a stale browser tab holding an older JS bundle from before a mid-session deploy, validating against outdated client rules while hitting the newly-deployed stricter server route. Since the specific rejected field was never previously shown to the user, the real culprit couldn't be confirmed — this fix makes the next occurrence (a retry, or any future submission failure) show exactly which field and why, instead of requiring another blind code audit.

- **3 new projects added from luxmark.com.pk** (MARK-1, MARK Skardu – Hotel & Resorts, Signature Hotels Islamabad), 1 new developer account (LUXMARK Pvt Ltd). **luxmark.com.pk is explicitly a marketing/broker company, not a developer** — its own About page states they're "real estate professionals... authorized marketing partner" reselling plots in Blue World City, Faisal Town Phase 2, Park View City, Capital Smart City, and RUDN Enclave (third-party societies developed by others). None of those five were added here — attributing them to Luxmark would be exactly the broker-self-attribution trap this session's conventions warn against, and re-cataloging them properly (real developer, phases, etc.) is a separate, larger task. Only the two projects LUXMARK genuinely develops itself under its own brand (MARK-1, MARK Skardu — independently confirmed via a third-party broker citation, not just Luxmark's own site) plus one project it merely markets for a developer already in this DB (Signature Hotels Islamabad, J7 Group — same developer as the existing Radisson BLU project, ownership cross-checked via Zameen.com and a second independent broker, zameenlelo.com) were added. **MARK-1** (University Town, Islamabad — new area added) has no published pricing anywhere reachable. **MARK Skardu** (Airport Road, Skardu — first-ever project for Skardu, so both the city and its first area were newly created; no more specific locality was published) has 2 real payment-plan rows (Hotel Suite PKR 11M, The Loft PKR 29M, both with a 12.5%/7.5%/30-monthly/20%-possession structure) sourced from luxmark.com.pk itself since it's LUXMARK's own project. **Signature Hotels Islamabad** (Top City 1, Islamabad) has 5 floor/tier-based payment_plans rows (per-sqft rates only, PKR 33,000–85,000/sqft) — no fixed unit sizes or total prices were published on any source found, and the per-sqft rates were cross-checked as identical between luxmark.com.pk and a second independent broker (zameenlelo.com) for 3 of the 5 tiers, with the other 2 sourced from zameenlelo.com only.

- **1 new project added over the user's explicit override: Nestora Glamps** (New Murree Resorts Phase 2, Angoori Road, Murree — both the city and area newly created, Murree's first project), 1 new developer account (Wirasat.com Developers). This is the one case this session where the developer-verification bar wasn't met before adding: Wirasat independently reads as a real estate **marketing agency** (not a builder — its own bio and third-party profile both describe it that way, and it separately runs FAQ/marketing pages for Blue World City, a society it doesn't develop, the same broker-pattern this session otherwise treats as disqualifying); the base "New Murree Resorts" scheme traces to a different company (Crown Estate and Marketing) per independent listings, with Wirasat specifically claiming only "Phase 2"; and the project's own sister page ("Iceland Galyat," the second of its two sites) is self-contradictory on NOC status (titled "NOC Approved" while the body text says the NOC "is in progress"). Flagged all of this to the user via `AskUserQuestion` and again after further digging (which surfaced the Crown Estate and NOC-contradiction findings, not resolving the concern); the user reviewed it and explicitly asked to add it anyway, citing Wirasat's own project page and a Facebook groundbreaking-ceremony post as sufficient. Added per that instruction, with every caveat above written directly into the project's `description` rather than presented as verified fact. Pricing (~PKR 4.6M starting price for a 2.5-marla dome, 15% down/10% possession, 6-month or 1-year timeline, "up to 47% annual return" claim) came from a mix of Wirasat's own site — exact installment figures are embedded in unreadable payment-plan images, not extractable as text — and independent broker citations for the base location; recorded as one indicative `payment_plans` row, explicitly marked as unverified rather than an official price list. The project spans two physical sites (Angoori Road, Murree and Iceland Galyat/Barrian, KPK) under one brand; only the Murree site got a structured `city_id`/`area_id` — the Galyat site is noted in the description only, not modeled as a second project row.

- **Faisal Hills (Islamabad) society filled out with full details and a payment plan**, at the user's request ("add complete details and payment plans"). The society row already existed as a one-sentence stub — this session found `societies` has no dedicated per-plot-size pricing table (unlike `projects`/`payment_plans`, which is scoped to `project_id` only, no `society_id`), so the payment-plan breakdown went into the `area_guides.content` TipTap JSON field instead, as headed prose sections (Blocks & Plot Sizes, Payment Plan, Notable Facilities) — the first `area_guides` row this DB has ever had (table was empty). Populated `developer_name` (Zedem International), `total_plots` (~24,000, single-source-family figure, flagged as approximate in the content), `total_phases` (7, matching its actual named blocks — Faisal Hills uses blocks, not DHA-style numbered phases), 7 real `society_blocks` rows (Executive, A, B, B-Extension, C, D, Hills Estate View), amenities, and approximate lat/lng (33.71, 72.79). `avg_price_marla` deliberately left null — block-to-block price variance is too wide (5 Marla alone ranges ~PKR 18-80 Lac depending on block) to responsibly collapse into one average. The society's own site (faisalhills.com) returned empty content on every fetch attempt (root, `/location/`, `/payment-plan/`) — all pricing was cross-checked across multiple third-party marketing-farm sites instead (skymarketing.com.pk, Zameen's area guide, faisalhillsmap.pk, and others), which is weaker sourcing than this session's usual "developer's own site" bar; one price example (10 Marla: PKR 1.05 Cr total, ~12.95 Lac down, 16×580K quarterly) reconciled to within 1% and was used with confidence, while a genuine cash-only-vs-installment contradiction on 1 Kanal plots across sources was written into the content as an open caveat rather than resolved by picking one side. Also flagged as a con in `area_guides.cons`: despite the "Islamabad" branding used everywhere (including this DB's pre-existing `city_id`), Faisal Hills sits on GT Road N-5 near Taxila, technically within Rawalpindi District, not Islamabad Capital Territory — left `city_id` as Islamabad (unchanged, matching how every source brands it) rather than unilaterally reclassify it. **Correction (same day):** the user asked to re-verify the developer name, suspecting "Faisal Town Group." Re-researched and confirmed both names are correct at different levels: **Zedem International Pvt Ltd was the name on Faisal Hills' own NOC filing**, and the company has since been **renamed Faisal Town Group Pvt Ltd** — same legal entity, same chairman (Chaudhry Abdul Majeed), same team, now used across the group's full portfolio (Faisal Town Phase 1 & 2, Faisal Hills, Faisal Residencia, Faisal Margalla City). Updated `developer_name` to `"Faisal Town Group (Zedem International)"` and reworded both `societies.description` and the `area_guides.content` opening paragraph to explain the name history explicitly, rather than presenting only the older legal name as if it were the current brand.

- **Free-tier listing cap temporarily removed, at the user's explicit request** ("we are in start... unlimited listings on free account... I will tell you when to implement [it back]"). `PLAN_LIMITS.free` in `app/api/listings/route.ts` changed from `5` to `999999` (matching the existing pattern already used for `elite`, rather than introducing a separate "unlimited" code path) — the enforcement check itself (`active_listings >= limit`) is untouched, so reinstating a real number later is a one-line change. Also updated the Free plan's feature list on the subscription/pricing page (`components/dashboard/subscription/SubscriptionClient.tsx`) from "5 listings" to "Unlimited listings" so the pricing page doesn't keep advertising a cap that no longer applies — leaving that stale would have undercut the point of the change (encouraging more free signups during this growth phase). Deliberately left the Pro-tier upsell card on `/dashboard` ("Get 50 listings...") unchanged — Pro still has real value beyond listing count (CRM pipeline, more photos, verified badge, featured credits) and rewriting that pitch felt out of scope for what was asked; flagged to the user as a possible follow-up rather than changed unprompted. No cap exists on developer-submitted `projects` (checked `app/dashboard/projects/actions.ts` — no limit there), so nothing to change on that side.

- **"Lahore, Karachi and Islamabad only" copy replaced with nationwide framing**, at the user's request — the site originally launched scoped to the 3 main cities, but now covers all of Pakistan (matching the growing city/area/society roster added throughout this session: Rawalpindi, Faisalabad, Multan, Peshawar, Quetta, Gujranwala, Sialkot, Abbottabad, Skardu, Murree). Grepped for `Lahore.*Karachi.*Islamabad`-style enumerations across the codebase and updated every hit to say "across Pakistan"/"nationwide" instead of naming exactly 3 cities: `lib/seo/metadata.ts` (`SITE_DESC`, plus 2 new keyword entries for Islamabad/Peshawar), `public/manifest.json` (PWA description, and the "Search Properties" shortcut which pointed at `/buy/lahore/` — changed to `/` since `/buy` has no city-agnostic route), `lib/notifications/email.ts` (the buyer welcome email, which already had "across Pakistan" phrasing for the *developer* welcome email as a precedent — the buyer one just hadn't been updated to match), and the meta descriptions on `/about`, `/agents`, `/blog`, and `/new-projects`. Also rebalanced `components/portal/Footer.tsx`'s `PROPERTIES_LINKS` (was almost entirely Lahore-flavored quick links with token Karachi/Islamabad mentions — spread across Lahore/Karachi/Islamabad/Peshawar/Rawalpindi now, and "All Listings" points at `/` instead of implicitly defaulting to `/buy/lahore`) and `CITY_LINKS` (added Peshawar and Quetta). Deliberately left city-specific deep links themselves alone (e.g. "Houses for Sale in Lahore") — those are legitimate SEO-valuable terms for whichever city they name, not a coverage claim; the issue was the copy implying only 3 cities exist, not the existence of city-specific links at all.

- **Lakeshore City (Khanpur Dam) society added with full details and a payment plan**, at the user's request. Developer: **Al Sadat Group (Al Sadat Marketing)** — verified as genuinely their own project (not a resold third-party scheme, the trap this session otherwise watches for): multiple independent broker sites and the developer's own site agree Lakeshore City is Al Sadat's *first self-owned, self-developed* project, distinct from their usual business of marketing other developers' societies. Founded 2015 as Al Sadat Associates, rebranded Al Sadat Marketing in 2018, under an older parent business dating to 1975 — flagged as a real-estate-*marketing* background rather than a construction track record in the society's `cons`, same caution applied to Luxmark/Wirasat earlier. **First-ever project for Haripur District, KPK**, so both `Haripur` (city) and `Khanpur Dam` (area) were newly created — the location sits ~50km from Islamabad despite frequent "Islamabad" branding on marketing sites (same pattern as Faisal Hills). NOC-approved by TMA Khanpur (No. 145-50), WAPDA-endorsed, 16,000 Kanal across three segments (Lakeshore Residencia, Lakeshore Farms, Lakeshore Club). **This is the best-sourced payment plan added this session**: fetched directly from the developer's own official payment-plan page (unlike Faisal Hills, which returned empty content on every fetch attempt), with every single row reconciling exactly to its stated total (booking % + confirmation % + monthly installments + balloting % = total, checked arithmetically before use) — recorded as a full breakdown in `area_guides.content` (Farm Houses with/without development charges, Commercial Plots, Executive Block Residential, Executive Block Commercial; 17 content blocks total). `avg_price_marla` left null — the project spans Kanal-sized farmhouse plots and Marla-sized residential/commercial plots, too heterogeneous to collapse into one figure, same reasoning as Faisal Hills.

- **Broadway Shopping Centre (Q-Links, Bahria Orchard Lahore) updated with a real, fully-reconciling payment plan**, at the user's request, replacing the earlier broker-sourced figures this session had left unresolved. The user supplied the project's own official payment-plan PDF (`qlinks.pk/wp-content/uploads/.../BSC-Payment-Plan-Jun26_compressed.pdf`), which turned out to be a genuine per-unit price list — 21 Ground Floor units (210-685 sqft) and 9 First Floor units (180-544 sqft), each individually reconciling exactly (Area × Rate = Total; 30% down + 36 monthly installments + 20% on possession = Total, verified arithmetically row by row) — the best-sourced payment plan found for any Q-Links project so far. Updated the existing 2 `payment_plans` rows (Ground Floor / First Floor, added when this project was first created) with real rate ranges (Rs 53,900-90,850/sqft Ground, Rs 37,400-62,100/sqft First), size ranges, and a concrete starting `total_price` per floor, rather than the previous `null` placeholders. Also re-verified the developer per the user's explicit ask ("search for real developers") — Q-Links is independently corroborated by 3+ third-party broker/marketing sites (CDB Real Estate, Bahria Town Consultant, Habibi Holdings) as an established Lahore developer with 20+ delivered projects, not just self-claimed on qlinks.pk — and added newly-found facts (13.5 Kanal total area, LDA + TMA approval) to the project description.

- **2 new projects added: Clock Tower and Times Square Mall & Residencia.** The user supplied a payment-plan PDF (`ClockTowerPaymentPlan.pdf`) that WebFetch couldn't parse (binary PDF) — extracted via PyMuPDF (`fitz`, already installed; `pdftotext`/`pdftoppm` are not) rendered to a PNG page image and read visually, since the raw text extraction came out with columns reordered/jumbled in a way that risked misreading which price belonged to which installment type. **Clock Tower** (Plot 7, opposite NUST Gate 2, H-13, Islamabad — new area) is a JV between BuildCo Pvt Ltd and EverGreen Developers Pvt Ltd, confirmed independently via BuildCo's own site (not just the PDF, which only brands EverGreen) plus iReal Projects and Towers.pk; new developer account created for EverGreen (reused for `developer_id`, BuildCo noted as JV partner in the description, same convention as The Pavilion's 3-developer JV earlier). All 6 unit-size payment-plan rows reconcile exactly at a flat PKR 14,500/sqft (booking 10% + confirmation 11% + 20×1% monthly + 3×10% mid-year + possession 29% = 100%, checked arithmetically). Flagged a real discrepancy rather than silently picking one number: iReal Projects cites a 409–1,200+ sqft unit-size range, while the official payment-plan PDF shows 300–944 sqft — used the PDF's numbers since they're what the payment_plans rows are sourced from and they reconcile. **Times Square Mall & Residencia** (Bahria Orchard Phase 4, Lahore) reuses the existing **Sheranwala Developers** account (already had Mayfair Residencia and Hawthorn Suites by Wyndham) — confirmed via Zameen.com and multiple brokers as the same company behind sheranwalagroup.pk/timessquaremarketing.com, and linked to the existing Bahria Orchard society. Residential units (1-bed, 2-bed) reconcile exactly at a flat PKR 19,500/sqft on an 18-month plan (15% booking + 15% down + installments); a separately-advertised 20%-down/48-monthly commercial structure was recorded as a distinct payment_plans row rather than treated as a contradiction, since residential and commercial wings commonly carry different terms — a cited Ground Floor shop price (PKR 17,690,000) had no matching published size, so no commercial per-sqft rate could be confirmed.

- **1 new project added over the user's explicit override: Canvas Mall & Residence** (Al-Kabir Downtown, Al-Kabir Town Phase-II, Main Raiwind Road, Lahore — linked to the existing Al-Kabir Town society), 1 new developer account (BYD Developers (Pvt) Ltd). Tried to use the pre-installed headless Chromium for this one since the user asked to "open your browser" — network sandboxing in this environment resets direct Chromium connections even through the proxy, so fell back to WebFetch/WebSearch (which worked fine, same as every other addition this session). Another developer-verification-bar case: no independent (non-BYD) source — no SECP record, no named leadership, no Zameen/Graana listing — confirms "BYD Developers" as a real, separate construction company; every other mention of "Canvas Tower"/"Canvas Mall" describes it only as sitting inside Al-Kabir Town Downtown, whose overall developer is the well-established, independently-verified Al-Kabir Developers, without crediting a specific sub-developer for this tower. Flagged this to the user via `AskUserQuestion` (options: add as BYD, reattribute to Al-Kabir Developers, or skip); they chose to add it with BYD Developers as stated, so it went in per that instruction with the verification gap written directly into the description rather than presented as confirmed fact. No pricing or payment plan published anywhere reachable — added with unit sizes only (Studio 275/300 sqft, 1-bed 450/550 sqft, 2-bed 800/900 sqft, plus commercial/food-court/office space).

- **1 new project added: Signature Townhouses** (Etihad Town Phase 1, Main Raiwind Road, Lahore — linked to the existing Etihad Town society), 1 new developer account (Rahat Developments (Rahat Marketing)). Surfaced from a promotional flyer the user shared ("A Project by Rahat Marketing," "Launching Soon") after an initial exploratory question about how to model a sub-developer's project inside a larger society (answer: the same `projects.society_id` pattern already used for MGC Jewel/Canvas Mall/Islamabad Square — no new modeling needed). Re-researched the developer per the user's prompt: two coordinated domains (rahatmarketing.pk, rahatdevelopments.pk) trace back to **Rahat Bakers**, a real, longstanding Pakistani bakery brand later diversified into real estate — a more specific, checkable origin story than the generic "marketing agency since [year]" boilerplate that made Wirasat/Luxmark cautionary cases, though still no SECP record or named leadership found independently, and the primary brand name is literally "...Marketing." Landed closer to the Atrium Hills tier (developer's-own-site-only sourcing accepted without a user override) than the Wirasat/Canvas Mall tier (explicit override needed) — added directly with the caution written into the description. Both `rahatmarketing.pk/signature-townhouses` and `rahatdevelopments.pk` returned server errors on every direct fetch attempt, so pricing (~PKR 10.9M starting, 950-1,000 sqft 3-bed townhouses, 15% booking + 2-year plan or a newer 3-year/30-month-possession plan) is sourced from search-indexed content rather than a verified primary page — recorded as one indicative `payment_plans` row and the project status set to `pre_launch` given the flyer's "Launching Soon" framing.

- **Etihad Town Sialkot society added with full details and a payment plan**, at the user's request, sourced initially from a broker page (propertyonline.pk) but cross-checked against the developer's own site (etihadtown.com.pk). **Confirmed independently as the same Etihad Group already behind this DB's existing Etihad Town (Lahore) society** — multiple sources describe Etihad Group as a diversified company (real estate, engineering, energy, steel, hospitality; 10,000+ employees; offices in Lahore, Sialkot, London, Dubai, Riyadh) with Etihad Town Lahore, Etihad Town Sialkot, and Etihad Garden (Rahim Yar Khan) all under one portfolio — not a broker-misattribution case at all, more like DHA/Bahria Town's multi-city pattern. **First-ever project for Sialkot city**, so "Daska Road" was added as its first area. Four real `society_blocks` rows (A, B, C, Executive). Payment structure (15% booking + 10% confirmation + 36 monthly installments (40%) + 15% ballot + 20% possession) confirmed identically by both the broker source and the developer's own site — a strong signal of accuracy — but the two sources cite different total prices for the same plot sizes (developer's site: 5 Marla 4.75M/10 Marla 9.3M/1 Kanal 18.2M; broker: 4.5M/8.9M/17.6M respectively). Used the developer's own, more authoritative figures as primary and noted the broker's lower figures as a likely-earlier price list in the content rather than silently discarding the discrepancy. Monthly installment amounts were computed from the confirmed percentage structure (not separately published as exact figures).

- **1 new project added: City Walk Faisal Hills** (entrance of Faisal Hills, GT Road, Islamabad — linked to the existing Faisal Hills society), reusing the existing **Faisal Town Group** developer account (the same company behind Faisal Hills itself). Found via linkers.com.pk, a marketing/broker platform, but the developer attribution (Faisal Town Group in JV with Square Four Developments, per an MOU) cross-checked independently — Square Four itself is described elsewhere as operating "within the Faisal Town Group ecosystem," so this reads as an internal group partnership rather than an outside broker-misattribution case, unlike Canvas Mall/Wirasat. A G+2 pedestrian-retail-street concept, 70 units across 2.5 Kanal, 162-415.62 sqft, floor-tiered pricing PKR 32,000-68,000/sqft (no fixed per-unit total published, recorded as a rate range in one `payment_plans` row). Payment: 30% down + 10% at grey structure + 10% at possession + remainder over a 4-year plan (exact installment schedule not published).

- **Lahore Smart City society added with full details and a payment plan**, at the user's request. Developer: **Habib Rafiq Limited under a Future Development Holdings (FDH) consortium** — the same group behind Capital Smart City Islamabad, independently well-established (no verification-bar concerns here, unlike Wirasat/Luxmark/BYD/Rahat). Master-planned by Surbana Jurong (Singapore). Resolved an apparent area discrepancy across sources rather than picking one: **20,000 Kanal currently acquired within a longer-term 50,000-55,000 Kanal planned footprint** — both figures are correct at different scales, not a contradiction. **First-ever project for this specific corridor**, so "GT Road" was added as a new Lahore area (matching Zameen's own categorization of the location). Four named blocks (General, Executive, Overseas, Overseas Executive — the Overseas blocks reserved for overseas Pakistanis) recorded as real `society_blocks` rows. Payment structure (10% booking + 10% confirmation + 12 quarterly installments over 3 years) verified arithmetically — every **residential** row reconciles to within 0.2% of its stated total — but **commercial** plot figures found (Executive/Overseas 4 & 8 Marla, Sport City) did not fully reconcile (roughly a 10% gap, likely a missing line item in the source), flagged explicitly as less-verified in both the description and `area_guides.cons` rather than presented with the same confidence as the residential figures.

- **Zaitoon City society added with full details, plus 3 linked projects**, at the user's request (zaitoon.com.pk). Developer: **Zaitoon Group**, founded 1987 by Mian Ayaz Anwar and Mian Ijaz Anwar — an established, independently-corroborated 35+ year developer, no verification-bar concerns. Sourced entirely from third-party cross-referencing since the developer's own site (zaitoon.com.pk) returned 403 Forbidden on every fetch attempt (root and subpages alike). Untangled a naming/scope ambiguity rather than guessing: **"New Lahore City" (Phases I-III) and "Zaitoon City" (Phase IV) are the same overall master scheme** marketed under two related names by the same group — modeled as one society (`Zaitoon City`), on Main Canal Bank Road/Sundar Road (new Lahore area), with 4 named enclaves (Diamond, Sapphire, Gold, Rose) recorded in the description. Residential plots (5/10 Marla, 1 Kanal) on a 3-year plan starting ~PKR 650,000/marla — flagged as an indicative starting rate only since no complete reconciling price table could be sourced anywhere. **3 new projects added, linked via `society_id`**: Zaitoon Premium Heights (mixed-use retail/residential), Zaitoon Signature Homes (5 Marla double-storey houses, Phase III & II Extension), and Zaitoon Executive Lodges (2 Kanal signature plots, Phase IV) — none had published pricing anywhere reachable, added with confirmed developer/location/unit-type data only. **Deliberately scoped out two other Zaitoon-branded developments that are NOT part of this location**: Zaitoon Lifestyle (a physically separate site on Jia Bagga Road near Lake City) and Zaitoon Plaza (Hall Road, central Lahore, 290 shops + 78 offices) — both real, same developer, but distinct land parcels that would need their own society/organic-location treatment rather than being folded into Zaitoon City's `society_id`. Noted as a known gap below.

- **1 new project added: Kuwait Mall** (Sector E, Bahria Town Lahore — linked to the existing Bahria Town Lahore society), 1 new developer account (UA Constructions). **This is the first case this session involving a formal, publicly-issued denial rather than just an unverifiable claim**: thekuwaitmall.com names "Izhar Group" as developer, but Izhar Construction (Pvt) Ltd has published a public notice explicitly disavowing any connection to this project, naming the real operator as "UA Constructions" and warning against further unauthorized use of the Izhar name. Two unrelated, legitimate "Izhar"-family developer accounts already existed in this DB from earlier sessions (Izhar Marketing → Heaven 20 Heights, Izhar Monnoo Developers → Dream Gardens Multan Phase 2) — neither is connected to this dispute. Flagged the finding to the user via `AskUserQuestion` before doing anything (skip / add as UA Constructions / dig further); they chose to add it with UA Constructions as developer, so it went in per that instruction with the full Izhar dispute documented in the project description rather than silently naming either company as if uncontested. UA Constructions itself could not be independently verified beyond a Facebook business page and a Bahria Town Lahore office address — no track record confirmed. Units start from PKR 24 Lac on an unspecified "3-year installment plan" — no further pricing breakdown published anywhere reachable.

- **"Lahore Organic Farms" / "Organic Farm Houses" (Barki Road) investigated and deliberately NOT added** — a step below every other case this session: no source (the hosting broker page, nor any independent search result) names an actual construction/development company at all. The one recurring name, "Yasir Brothers Real Estate," independently confirmed as an estate agency/consultancy (est. 2008) that brokers multiple unrelated farmhouse schemes (Golf Residencia Farm House Society, Orchard Greenz, this one) — the same broker-markets-many-schemes pattern as Luxmark/Wirasat, not a builder of any one of them. No NOC found either, which matters more than usual for a "farm house" scheme on agricultural-zoned land, a known conversion-approval grey area in Pakistan. Flagged to the user with the reasoning rather than silently skipped or silently added under a broker's name.

- **1 new society added: Al-Hussain Villas** (near Pak Arab Housing Society Phase 2, off Ferozepur Road, Lahore — new area), developer Al Hussain Developers — verified via the developer's own official site (alhussainvillas.com) plus independent Zameen.com listings confirming multiple blocks are actually built and occupied ("now occupied by families"), a stronger signal than a marketing-only company. Seven named sub-developments recorded as real `society_blocks` rows (Block A, Block A Extension, Block B, Block B Extension, Smart Villas Sector C, Smart Homes, Townhouses). No NOC-approving authority or number found published anywhere reachable, flagged as a gap rather than assumed. `total_plots` deliberately left null — the one total-plot figure found (~1,000) applies specifically to Block B Extension, not a verified society-wide count, and using it as the total would have overstated confidence in a number that doesn't mean what it looks like it means. A specific broker-quoted payment example (3 Marla, PKR 3,000,000) was checked arithmetically and fell ~23% short of its own stated total, so it was excluded rather than used.

## Known gaps (not yet done)
1. **A few project addresses are ambiguous and were deliberately left unlinked** rather than guessed: "Skyline Boulevard" (Sector E, Lahore — possibly a Bahria Town sector), "Hassan Villas" (Jubilee Town Housing Scheme — may warrant its own society row later if more projects land there), "Calachi Icon" (inside Calachi Housing Society, Gulshan-e-Iqbal, Karachi — same reasoning, worth a society row if more projects land there). Similarly, **Zaitoon Lifestyle** (Jia Bagga Road, near Lake City, Lahore) and **Zaitoon Plaza** (Hall Road, central Lahore) are real Zaitoon Group developments on physically separate land from the Zaitoon City society — not yet added, would need their own society/organic-location treatment rather than being folded into Zaitoon City.
2. **Listing (`/listing/[slug]`) inventory is thin** (2 real listings vs. 63 projects) — this is *why* the stats fix above folds `projects` into the "Listings" stat rather than leaving it a near-empty number. The page itself is solid (RealEstateListing schema, similar-listings, internal-linking block) — the gap is real owner/agent listings, not code. Developer *projects* are fair game to catalog from public marketing material; individual *listings* need real agent/owner-submitted inventory — do not scrape competitor portals (Zameen, Graana, etc.) to fill this in, that would be misappropriating their content.
3. **Societies admin form still has no phases/blocks management UI** (the `society_phases`/`society_blocks` tables from migration 013 are still agent-extensible-only, populated via the listing form's "add new" flow) and no lat/lng map picker (image upload and edit page are done, see above).
4. **`cities.listing_count` / `areas.listing_count` / `societies.listing_count` are all the same kind of dead stored-counter column** (`DEFAULT 0`, no trigger) — only `get_public_stats()`'s cities figure has been fixed to bypass it (via a live UNION query) so far; anywhere else in the codebase still reading `.listing_count` directly off these tables is reading a stale/frozen number, not a real one.
5. **Developer dashboard has no Analytics/CRM/Subscription/Boost Center equivalents** — those stayed agent-only nav items; developers only got Dashboard, My Projects, Add Project, Lead Inbox, Settings. Also no bulk-edit or duplicate-project-from-existing flow, and a developer can't manage `payment_plans` display order (always insert order = form order).

## Conventions established this session
- Never attribute a project to a marketing/broker site (AIWA, Titanium Agency, Sky Marketing, etc.) as its developer — find the real builder and cross-check via the developer's own site or 2+ independent sources before writing `developer_id`.
- Reuse an existing developer account if the project belongs to a company already in the DB. Don't create duplicate accounts for spelling variants of the same company (e.g. "RealTek Developers" / "Realtek Properties" / "RealTech Properties" is one developer, not three).
- New developer accounts: real Supabase Auth users (`crypt()`/pgcrypto bcrypt, pre-confirmed email), `{slug}@zproperty.pk` placeholder email, sequential `+9230000001xx` placeholder phone (check `SELECT phone FROM users WHERE phone LIKE '+92300000%' ORDER BY phone` for the next free one), random 12-char password. Credentials are shown once in chat only — not recoverable after — so the user needs to save them immediately.
- Never fabricate pricing, unit sizes, or dates that aren't published anywhere. Leave the field null and say so in the description, rather than estimate.
